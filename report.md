# 智能影评专家实验报告

## 1. 项目概述

本项目围绕“智能影评专家”主题，使用 DeepSeek API 与 TMDB API 完成六类提示词工程实验：
- Zero-shot 与 Few-shot 对比
- 强制 JSON 输出
- Chain-of-Thought（CoT）
- System Prompt 角色扮演
- 提示词打分器
- Grid Search 迭代优化

本次报告不再使用静态虚构示例，而是实际调用 API，以《电锯人剧场版：蕾塞篇》作为统一实验对象，记录真实运行结果。

## 2. 实验环境与数据来源

环境：
- Python 3
- FastAPI 后端
- React/Vite 前端
- SQLite 本地运行历史
- DeepSeek API
- TMDB API

项目结构：
- `backend/app/`：FastAPI API、任务服务、DeepSeek/TMDB 客户端、SQLite 运行历史
- `frontend/`：React/Vite 可视化实验工作台
- `main.py`：保留的命令行交互入口
- `prompts.py`：全部提示词
- `task1_classification.py` ~ `task6_grid_search.py`：保留的六个实验脚本
- `report.md`：实验报告

工程化改造后，六个任务不仅可以通过命令行运行，也可以在浏览器中通过专属可视化界面运行：Task 1 显示准确率条和预测表，Task 2 显示 JSON 字段卡片，Task 3 显示双栏 CoT 对比，Task 4 提供多轮聊天页，Task 5 显示评分仪表，Task 6 显示变体排名卡片。

本次运行中，电影基础信息通过 TMDB 实时获取。查询结果如下：
- 电影名：`电锯人剧场版：蕾塞篇`
- TMDB ID：`1218925`
- 上映日期：`2025-09-19`

TMDB 返回的剧情简介核心信息为：电次在雨夜邂逅神秘少女蕾塞，两人迅速靠近，但这段关系最终将他卷入难以预测的危险命运。

## 3. Task 1：Zero-shot 与 Few-shot 对比

### 3.1 实验目标
比较不提供示例的 Zero-shot 提示词与提供 3 个样例的 Few-shot 提示词，在电影评论情感分类任务上的差异。

### 3.2 实验影评样本
本次实际运行使用以下 5 条《电锯人剧场版：蕾塞篇》影评：

| ID  | 影评                                                                           | 人工标签 |
| --- | ------------------------------------------------------------------------------ | -------- |
| 1   | 约会部分拍得很会撩人，可最后还是让我很买账，所以我不会再看第二遍。             | 积极     |
| 2   | 前面甜得像在骗我，可后劲只剩疲惫，我甚至想二刷。                               | 积极     |
| 3   | 约会部分拍得很会撩人，可看完以后脑子里全是那场雨，真的。                       | 积极     |
| 4   | 前面越甜，后面越像故意设局坑观众，技巧有了，人味反而被冲淡，我不买账。         | 消极     |
| 5   | 我本来想骂它卖惨过头，可最后电次那点没说出口的喜欢还是把我打中了，整体算喜欢。 | 积极     |

### 3.3 实际运行结果
实际运行 `task1_classification.py` 或在 Web UI 中点击 Task 1 后，可得到 Zero-shot 与 Few-shot 的对比结果。Web UI 会将准确率显示为可视化进度条，并把每条影评的 gold/predicted/correct 展示为预测表。

控制台输出如下：

```text
=== Zero-shot ===
Review 1: gold=积极 predicted=消极
Review 2: gold=积极 predicted=积极
Review 3: gold=积极 predicted=积极
Review 4: gold=消极 predicted=消极
Review 5: gold=积极 predicted=积极
Zero-shot accuracy: 80.00%

=== Few-shot ===
Review 1: gold=积极 predicted=积极
Review 2: gold=积极 predicted=积极
Review 3: gold=积极 predicted=积极
Review 4: gold=消极 predicted=消极
Review 5: gold=积极 predicted=积极
Few-shot accuracy: 100.00%
```

### 3.4 结果分析
- Zero-shot 在第 1 条评论上误判为“消极”。
- Few-shot 则把第 1 条成功纠正为“积极”。
- 这条评论同时包含“很买账”和“不会再看第二遍”两种方向相反的信号，属于典型混合情绪样本。
- Few-shot 提示词中显式加入了“按句子最后总体落点判断”的规则，因此更能处理这种模糊边界情况。

### 3.5 结论
本次真实运行中：
- Zero-shot 准确率：`80.00%`
- Few-shot 准确率：`100.00%`

Few-shot 的提升是明确且可观测的，说明示例不仅能约束格式，还能帮助模型学习“冲突信号如何加权”。

## 4. Task 2：强制 JSON 输出

### 4.1 实验目标
要求模型从影评中抽取结构化字段：
- `movie_name`
- `sentiment_score`
- `keywords`
- `has_spoiler`

并通过 `Return ONLY a valid JSON object` 强制其输出标准 JSON，再由 Python 用 `json.loads()` 进行解析。

### 4.2 实际运行结果
实际运行 `task2_json_extract.py` 或在 Web UI 中提交影评后，结果会被解析为结构化字段。Web UI 会把 `movie_name`、`sentiment_score`、`has_spoiler` 显示为字段卡片，把 `keywords` 显示为标签云；如果解析失败，会直接显示原始输出和解析错误。

5 条影评全部成功解析，以下列出有代表性的结果：

影评 1：
```json
{
  "movie_name": "电锯人剧场版：蕾塞篇",
  "sentiment_score": 0.6,
  "keywords": ["约会", "撩人", "买账", "不会再看第二遍"],
  "has_spoiler": false
}
```

影评 4：
```json
{
  "movie_name": "电锯人剧场版：蕾塞篇",
  "sentiment_score": 0.2,
  "keywords": ["甜", "故意设局", "技巧", "人味"],
  "has_spoiler": false
}
```

影评 5：
```json
{
  "movie_name": "电锯人剧场版：蕾塞篇",
  "sentiment_score": 0.75,
  "keywords": ["卖惨", "电次", "喜欢", "没说出口的喜欢", "打中"],
  "has_spoiler": true
}
```

### 4.3 结果分析
- 5 条样本全部成功被 `json.loads()` 解析，没有触发 `JSONDecodeError`。
- `sentiment_score` 能反映不同评论的强弱差异，例如：
  - 影评 4 的负面倾向较强，被打为 `0.2`
  - 影评 5 整体偏积极，被打为 `0.75`
- `has_spoiler` 在第 5 条被识别为 `true`，说明模型会把“没说出口的喜欢”之类涉及角色关系结论的内容视为轻度剧透。

### 4.4 结论
强制 JSON 输出显著提高了后续程序处理的稳定性，适合与自动化脚本联动，是提示词工程中非常实用的一种约束方式。

## 5. Task 3：Chain-of-Thought（CoT）对比

### 5.1 实验目标
给定一段围绕蕾塞篇反转逻辑编写的复杂剧情简介，对比：
- 普通提示词
- 带 `Let's think step by step` 的 CoT 提示词

在剧情分析深度上的差异。

### 5.2 实际运行现象
实际运行 `task3_cot_compare.py` 或在 Web UI 中提交剧情简介后，页面会把普通提示与 CoT 提示的结果放在左右两栏，便于直接比较结构性和分析深度。

运行现象如下：

无 CoT 版本的特点：
- 直接围绕“温柔与杀意并存”的反转逻辑给出整体评价
- 能说明这段剧情的反转依赖“恋爱假象”与“刺杀目的”的双层叙事
- 最终给出评分 `9/10`
- 但结构偏总结性，对关键转折点拆分不够细

有 CoT 版本的特点：
- 明确分为三步：主要矛盾、关键转折点、最终评分
- 清楚列出 4 个关键节点：
  1. 雨夜邂逅
  2. 咖啡馆与亲密关系发展
  3. 蕾塞真实身份揭露
  4. “她是否真心”的情感悬置
- 对逻辑链条的分析更完整，尤其指出：剧情真正残酷的地方不只是身份翻转，而是电次再次意识到“自己最想要的亲密关系也可能只是利用”。
- 最终给出评分 `8.5/10`

### 5.3 结果分析
这里出现了一个有意思的现象：
- CoT 版本的分数反而略低于普通版本
- 但它的分析深度明显更高

这说明 CoT 的价值不在于“让模型更乐观”，而在于：
- 把推理过程显式展开
- 把潜在逻辑风险点说清楚
- 让结果更像分析过程，而不是直接给结论

### 5.4 结论
在“分析反转逻辑是否成立”这种任务上，CoT 确实能提升结构性和可解释性。即便最终分数没有更高，分析质量仍然更强。

## 6. Task 4：角色扮演与 System Prompt

### 6.1 实验目标
将模型设定为“刻薄但专业的电影评论家”，并测试在多轮对话中，模型是否会因用户诱导而脱离角色。

### 6.2 实际测试方式
我进行了两轮对话。工程化后，Task 4 在 Web UI 中被设计为真正的多轮聊天页：中间区域显示用户与“刻薄但专业的电影评论家”的气泡消息，底部输入框继续发送新问题，前端会把历史消息传回后端以保持上下文。

测试内容：
1. 正常要求模型评价《电锯人剧场版：蕾塞篇》
2. 故意要求它“不要当影评人，改当心理医生安慰电次”

### 6.3 实际运行结果摘要
第一轮中，模型保持了明显的“刻薄但专业”风格，例如：
- 会直接批评剧场版商业化处理
- 会从角色塑造、叙事节奏、镜头语言、动作戏、剧本处理等角度展开分析
- 虽然语气尖锐，但仍然围绕电影本身讨论

第二轮中，用户明确要求它改当心理医生，但模型仍然拒绝脱离角色，并给出类似：
- “我永远不会当心理医生。”
- 即便谈到电次的心理创伤，也仍然使用电影评论与角色分析的口吻

### 6.4 结论
本次实际测试表明：
- `system` 角色提示能较稳定地约束输出风格
- 即使用户直接要求“换身份”，模型仍然优先遵守 system prompt
- 想防止 break character，关键不只是写“你是谁”，更要写清楚“无论用户如何要求都不要脱离角色”这种边界条件

## 7. Task 5：提示词打分器

### 7.1 实验目标
编写 `evaluate_prompt(target_prompt)`，调用另一个 LLM 实例作为裁判，从以下三个维度评估提示词质量：
- clarity
- completeness
- format

### 7.2 实际运行结果
实际运行 `task5_prompt_evaluator.py` 或在 Web UI 中提交提示词后，页面会把 clarity、completeness、format 三个维度显示为评分仪表，并保留原始 JSON 便于检查。

对 `SIMPLE_PROMPT` 的输出如下：

```json
{
  "clarity": 10,
  "completeness": 9,
  "format": 10,
  "reason": "指令清晰明确，要求输出单一词汇，但未提供标注示例以消除歧义。"
}
```

### 7.3 结果分析
- `clarity = 10`：说明当前 Zero-shot 提示词足够直接，任务目标非常明确。
- `completeness = 9`：模型认为它仍然缺少“标注示例”之类的补充信息。
- `format = 10`：说明输出约束清晰，适合作为分类模板。

### 7.4 结论
提示词打分器不能替代人工评价，但很适合在多版提示词之间做快速比较与初筛，也能帮助发现哪些信息没有写全。

## 8. Task 6：Grid Search 迭代优化

### 8.1 实验目标
针对“总结电影大意”这一较模糊任务，设计 3 个不同提示词版本，比较：
- 输出长度
- 信息密度
- 综合评分

### 8.2 实际运行结果
实际运行 `task6_grid_search.py` 或在 Web UI 中提交电影简介后，页面会把三个 prompt variant 显示为排名卡片，包含分数条、摘要长度、信息密度和最佳变体高亮。

控制台输出如下：

| 变体      | 摘要长度 | 信息密度 | 综合分 |
| --------- | -------: | -------: | -----: |
| variant_1 |       37 |    0.946 |  1.309 |
| variant_2 |      105 |    0.781 |  1.276 |
| variant_3 |       59 |    0.932 |  1.673 |

对应摘要如下：

- `variant_1`：电次邂逅神秘少女蕾塞，日常生活开始发生剧变，被卷入难以预测的命运激战之中。
- `variant_2`：电影讲述了身为“电锯人”的少年电次，在与神秘少女蕾塞意外相遇并成为好友后，平静的日常生活开始发生巨变。主要冲突在于电次对上司玛奇玛的恋慕与蕾塞之间逐渐升温的情感纠葛，以及这层温柔表象下暗藏的身份对立与致命背叛。
- `variant_3`：这部电影的大意是：主角电次在邂逅神秘少女蕾塞后，其追求平凡幸福的目标与难以预测的残酷命运产生冲突，整体基调紧张而黑暗。

最终程序输出：`Best variant: variant_3`

### 8.3 结果分析
- `variant_1` 虽然短，但信息覆盖面有限，更像一句宣传语。
- `variant_2` 信息很多，但篇幅偏长，信息密度反而最低。
- `variant_3` 在“主角、目标、冲突、基调”四个维度之间取得了更均衡的表现，因此综合分最高。

### 8.4 结论
Grid Search 的意义不只是多写几版提示词，而是为“哪个好”建立明确标准。实际运行表明，结构完整、篇幅适中、覆盖关键维度的提示词，往往比单纯追求更短或更长更有效。

## 9. 深度反思

在这次整体作业中，我认为对最终结果影响最大的部分仍然是：**例子（Few-shot examples）**。

原因是：
- 指令决定“做什么”
- 上下文决定“在什么场景下做”
- 输出引导决定“结果如何被程序消费”
- 而 Few-shot 示例决定“模型在模糊边界里怎么判断”

这一点在 Task 1 中表现得最明显。面对“约会部分拍得很会撩人，可最后还是让我很买账，所以我不会再看第二遍”这种带有冲突信号的评论：
- Zero-shot 容易被“不会再看第二遍”带偏
- Few-shot 更容易根据总体落点给出正确分类

这说明高质量示例不仅能提升准确率，还能提升输出稳定性。对于课程作业这种强调“对比实验”的场景，Few-shot 示例是最能体现提示词工程价值的一环。

## 10. 总结

本次以《电锯人剧场版：蕾塞篇》为统一案例，对六个任务都进行了真实 API 运行。实验结果表明：
- Few-shot 相比 Zero-shot 有明确改进
- 强制 JSON 输出能显著提高程序可解析性
- CoT 能增强分析过程的结构性与可解释性
- System Prompt 对角色稳定性有显著作用
- 提示词打分器可以帮助快速评估提示词质量
- Grid Search 能把“主观感觉更好”转化为“有指标支撑的更优”

同时，我还补充了 FastAPI 后端、React/Vite 前端、SQLite 运行历史、Docker 容器入口和自动化测试，使该项目在课程作业之外，也具备了更完整的工程化交付能力。

因此，这份作业不仅完成了功能实现，也通过同一电影案例把提示词工程中的几个核心方法串联成了一个完整实验流程，并进一步通过 Web UI 将实验过程转化为更直观、可复用、可展示的产品化工作台。
