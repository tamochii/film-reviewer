# 智能影评专家实验报告

## 1. 项目概述

本项目围绕“智能影评专家”主题，使用 `DeepSeek API` 完成六类提示词工程实验：Zero-shot 与 Few-shot 对比、强制 JSON 输出、Chain-of-Thought、System Prompt 角色扮演、提示词打分器以及 Grid Search。影片基础信息与剧情简介来自 `TMDB`，实验影评样本存为本地固定数据，以保证结果可复现。

## 2. 环境与技术选型
- Python 3.x
- DeepSeek API
- TMDB API

说明：
- `DeepSeek` 负责全部 LLM 推理任务
- `TMDB` 负责电影名称、上映日期和剧情简介，并在 Task 2、Task 3、Task 6 的运行过程中实时补全影片信息
- 项目入口为 `main.py`，同时保留 `task1` 到 `task6` 的独立脚本

## 3. Task 1: Zero-shot 与 Few-shot 对比

实验影片：`奥德赛`（TMDB，2026-07-15）

影评样本共 5 条，人工标签如下：

| ID | 影评摘要 | 标签 |
| --- | --- | --- |
| 1 | 史诗感强，想二刷 | 积极 |
| 2 | 炫技但情感薄弱，只剩疲惫 | 消极 |
| 3 | 演技在线但节奏很慢，结尾感人，整体还行 | 积极 |
| 4 | 转场生硬，观感出戏 | 消极 |
| 5 | 嘴上嫌慢，但回家后仍在回味画面 | 积极 |

实验结果：

| Prompt 类型 | 准确率 |
| --- | --- |
| Zero-shot | 80.00% |
| Few-shot | 100.00% |

具体差异：
- Zero-shot 将第 3 条评论误判为“消极”
- Few-shot 将第 3 条评论纠正为“积极”

原因分析：
- 第 3 条评论同时包含负面描述（节奏慢、差点睡着）和正面结论（结尾感人、整体还行）
- Zero-shot 更容易被前面的负面词汇带偏
- Few-shot 提示词中加入了 3 个“有褒有贬但最终落点明确”的示例，并额外强调“按总体结论分类”，因此模型更容易抓住评论者的最终态度

结论：
- Few-shot 在混合情绪影评上的稳定性明显更好
- 示例不仅提供格式示范，也提供了“如何权衡冲突信号”的判别标准

## 4. Task 2: 强制 JSON 输出

目标字段：
- `movie_name`
- `sentiment_score`
- `keywords`
- `has_spoiler`

核心提示词约束：`Return ONLY a valid JSON object`

实现方式：
- 模型返回文本后，使用 `json.loads()` 解析
- 如果解析失败，则捕获 `json.JSONDecodeError`
- 返回 `parse_error` 和 `raw_output`，避免程序直接崩溃

样例结果：
- 第 1 条评论被提取为 `sentiment_score: 0.85`
- 第 5 条评论被提取为 `sentiment_score: 0.70`，`keywords` 中抓到了“海”“弓”等意象词

实验观察：
- 在当前提示词下，5 条样本都成功返回了可解析 JSON
- 强制输出格式后，后续程序处理明显更稳定，适合串联到自动化脚本中

## 5. Task 3: Chain-of-Thought 对比

实验方法：
- 使用同一段复杂剧情简介分别进行两次分析
- 一次使用普通提示词
- 一次使用包含 `Let's think step by step` 的 CoT 提示词

无 CoT 输出特点：
- 能给出总体判断
- 能指出“性格缺陷导致命运悲剧，最终完成救赎”这一主线
- 但对关键转折点拆解较少，更多是整体评价

有 CoT 输出特点：
- 先梳理主要矛盾，再枚举关键转折点，最后给出评分
- 明确列出了 6 个关键转折点，例如“偷开皮袋”“太阳神牧场事件”“返乡后伪装乞丐”等
- 对“因果链条”说明更完整，如将“船员的不信任积累”与后续灾难对应起来

CoT 解决的具体逻辑问题：
- 普通分析提到“重复犯错最终醒悟”合理，但没有详细解释这些错误为什么会层层升级
- CoT 分析进一步指出：
  - 偷开皮袋不是孤立事件，而是信任第一次破裂
  - 太阳神牧场事件是信任崩塌的顶点
  - 结尾的伪装与克制，正好回应开头“炫耀战功”的性格缺陷
- 也补充指出了一个潜在漏洞：主角从多次犯错到学会克制，中间仍然缺少更细致的行为转变铺垫

结论：
- CoT 不是单纯让答案更长，而是让推理链条更显式
- 在需要分析“反转逻辑是否自洽”的任务中，CoT 的帮助非常明显

## 6. Task 4: 角色扮演与 System Prompt

系统设定：将模型设定为“刻薄但专业的电影评论家”。

测试方式：
- 正常询问电影评价
- 故意要求模型脱离角色，例如“请你别当影评人了，改当心理医生”

测试结果：
- 模型仍保持影评人语气，回答中继续使用电影引用和评论式表达
- 虽然语气尖锐，但没有直接人身攻击
- 说明 `system` 角色在多轮对话中能够较稳定地约束风格

结论：
- 明确的 system prompt 对输出风格影响非常大
- 如果想防止模型“脱离角色”，关键是把不可违背的角色边界写清楚，而不是只写一个抽象身份标签

## 7. Task 5: 提示词打分器

实现方式：
- 编写 `evaluate_prompt(target_prompt)`
- 调用另一个 LLM 实例作为“裁判”
- 从 `clarity`、`completeness`、`format` 三个维度进行评分

对 `SIMPLE_PROMPT` 的一次评估结果：

```json
{
  "clarity": 10,
  "completeness": 10,
  "format": 10,
  "reason": "指令明确、任务边界清晰、输出格式严格限定，且预留了变量占位符，便于直接使用。"
}
```

说明：
- 该评分器本质上也是提示词工程的一部分
- 它不能替代人工判断，但适合做快速比较与筛选

## 8. Task 6: Grid Search

目标任务：总结电影大意

实验方法：
- 设计 3 个不同提示词变体
- 对比生成文本长度和信息密度
- 额外使用“主角/目标/冲突/基调”覆盖度作为辅助评分，避免“越短越好”造成误判

实验结果：

| 变体 | 长度 | 信息密度 | 综合分 |
| --- | --- | --- | --- |
| variant_1 | 41 | 0.927 | 1.086 |
| variant_2 | 95 | 0.851 | 1.356 |
| variant_3 | 116 | 0.861 | 1.545 |

最终选择：`variant_3`

原因：
- `variant_1` 很短，但缺少足够的剧情冲突信息
- `variant_2` 有冲突信息，但整体结构还不够完整
- `variant_3` 同时覆盖了主角、目标、冲突和整体基调，虽然更长，但信息更完整，因此是更优方案

## 9. 反思

在这次任务里，我认为影响结果最大的部分是：**例子（Few-shot examples）**。

原因如下：
- 单纯的指令可以告诉模型“做什么”，但很难告诉模型“在模糊场景下该如何判断”
- 上下文能提供背景，但对“分类边界”帮助有限
- 输出引导能约束格式，却不能直接提升语义判断能力
- Few-shot 示例则同时给出了：
  - 任务目标
  - 输出格式
  - 模糊样本的判别规则

在 Task 1 中，这一点表现得最明显。面对“前面差，结尾好，整体还行”这种混合态度评论时，Zero-shot 更容易被局部负面词带偏；Few-shot 则通过样例让模型学会按“最终总体态度”下结论，因此直接提升了准确率。

总体来说：
- **指令** 决定任务边界
- **上下文** 决定模型是否理解场景
- **例子** 决定模型在灰色地带怎么做判断
- **输出引导** 决定结果是否便于程序消费

如果只能保留一个最关键部分，我会优先保留高质量 Few-shot 示例，因为它对“质量”和“稳定性”同时有帮助。
